export const metadata = {
  title: "Hackwise 2.0 Registration | Campus Ambassador Referral",
  description: "Register for Hackwise 2.0 through a Campus Ambassador referral link. Join the national-level hackathon and compete for ₹60,000 prize pool.",
  robots: {
    index: false,
    follow: true,
  },
};

export default function ReferralLayout({ children }) {
  return children;
}

